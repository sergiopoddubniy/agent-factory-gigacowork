# Журнал версий

## V3.3 · 2026-09-25

Витринная сборка для платформы и политика обезличивания. Повод: платформа
ставит плагин только на весь продукт (отдельного тенанта нет), а в первой
сборке увидела референсы, которые «нельзя светить». Решение: GitHub
остаётся полным и накапливает референсы (пример, шаблоны, прогоны) —
принципиальная позиция автора; на платформу уходит отдельная сборка без
референсов и без провенанса, собираемая скриптом и проверяемая шлюзом.

- **Обезличивание репозитория.** Найдены и заменены реальные названия
  клиентов и контрагентов, пережившие первый проход (в `evidence_log.md`,
  `few_shot_patterns.md`, `scenario_template.md`, `agent_card_kit.md`,
  `agent_package.md`, `agent_templates/`, `platform_architecture.md`,
  `knowledge/README.md`, фикстуре `check_run.py`): условные Клиент-П/Б/Э/Д/Т/Н/Ф,
  «Пример-Контрагент», «Пример Технологии», `example-*.ru`; абсолютные
  суммы боевых прогонов заменены относительными; вариант аббревиатуры
  клиента, пропущенный ранее, приведён к Клиент-М.
- `docs/06_Политика_обезличивания.md`: три класса содержимого (запрещено
  везде / провенанс — только GitHub / разрешено везде), псевдонимы, правило
  написания нового урока, режимы шлюза, где живут референсы.
- `scripts/hub_gate.py`: шлюз запретов — даты, суммы, домены вне белого
  списка, ФИО, орг-формы с названием, стоп-слова, псевдоним рядом с датой;
  строгий режим для витрины, мягкий — для репозитория (CI); приватный
  стоп-лист вне Git.
- `scripts/build_hub.py`: витринная сборка в раскладке плагина платформы
  (шаблоны — `templates/hub/`: `plugin.yaml`, `agents/agent-factory.md`,
  `agent.html`, `README.md`, получены от платформы 25.09.2026): без
  `examples/` и шаблонов базы (`--с-примерами` возвращает), копилки пусты,
  провенанс вырезан построчно (в коде строки со строковыми литералами не
  трогаются — там фикстуры), команды в формате платформы, архив с UTF-8
  именами средствами Python; внутри сборки — строгий шлюз и
  `selftest_delivery.py`, `selftest.py` — на источнике; отчёт
  `СБОРКА_ВИТРИНЫ.md` с SHA-256.
- CI: job `hub` собирает витрину на каждый push и кладёт архив артефактом;
  мягкий шлюз на репозитории.
- `docs/03`: раскладка плагина зафиксирована, чек-лист обновлён, 18
  вопросов к платформе — в бэклог; `docs/07_Поставка_витрины.md` — что
  изменилось относительно первой сборки и почему этим можно пользоваться.
- `selftest.py` 118 (+13: шлюз в обоих режимах, витрина — раскладка,
  отсутствие референсов, UTF-8 имена, нетронутые фикстуры, пустые копилки,
  формат команд, `--с-примерами`); `selftest_delivery.py` 411/411,
  `mutate_landed` 118/118.

## V3.2 · 2026-09-24

Критерий детерминированного слоя расширен — синхронно с исходным скиллом
v9.11 (`25a74f69`). Повод общий: агент реестра закупок (48 карточек из 285)
собрали как «модель читает страницу», потому что критерий звучал «harness
применим, если есть арифметический инвариант», а агент ничего не вычислял;
задачу решил скрипт консультанта за минуты, одинаково в каждом прогоне.
Инвариант был — соответствия источнику. Седьмая находка, крупнее шести
предыдущих.

- `references/agent_package.md`, `references/computational_core.md` —
  редакции v9.11 дословно: **шесть видов инварианта** (арифметический,
  соответствия источнику, полноты множества, тождества ключа,
  идемпотентности, порядка и диапазона); **тест на исполнителя по шагам, а
  не по агенту** (воспроизводим без модели · есть инвариант · видна ли
  ошибка в ответе · требуется ли суждение); правило «шаг, воспроизводимый
  кодом, модели не поручается»; архитектура «детерминированный источник плюс
  агент сверху» — штатная, с признаками «собрано „модель читает страницу“»;
  пустой результат парсера — состояние; отказ записывается с названным
  инвариантом. Плюс фабричный абзац: где решение живёт в спецификации.
- Новый справочник фабрики `references/extraction_core.md` — извлекающий
  слой: `rules/поля.yaml`, движок, пять правил (пустота двух видов и
  BLOCKING при изменившейся вёрстке; регрессия на снимках источника;
  полнота по конструкции; незнакомая метка — вопрос; агент не «дочитывает»
  поля мимо парсера); исполняемость — песочница или MCP-коннектор.
- Спецификация: подмаркер **`Граница код/модель:`** в блоке 8 — таблица
  «шаг → исполнитель → инвариант → чем проверяется», строка на каждый шаг;
  строка **`Ядро:`** в блоке 6 (`нет — причина: …` / `расчётное` /
  `извлекающее`). `factory_common.boundary_rows()`, `core_kind()`; ворота
  `Ф-08-ГРАНИЦА` (таблицы нет, строки неполны, строк меньше шагов) и
  `Ф-06-ЯДРО` (строки нет, без причины, противоречие таблице).
  `build_agent_package.py` переносит таблицу в §6 навыка агента;
  `build_delivery.py` читает `Ядро:` (расчётное → Я10; оба вида — шаг
  инструкции по сборке про размещение ядра, строка в `СБОРКА.md`).
- `SKILL.md`: правило «Разделение труда между кодом и моделью — по шагам, а
  не по агенту» (4 984 токена, в норме); `pipelines/single_agent.md` — абзац
  v9.11 в Стадии 1, стадия 2 ссылается на решение.
- Проверки: `selftest.py` 105 (+7), `selftest_delivery.py` 409 (иглы v9.11:
  «Шесть видов инварианта», «шаг, воспроизводимый кодом, модели не
  поручается», «Разделение труда между кодом и моделью», «Отказ от
  детерминированного слоя — тоже решение» + иглы `extraction_core.md`),
  `mutate_landed` 118/118.
- Пример `1c-dev-assistant`: спецификация S8 (таблица границы по пяти шагам,
  `Ядро: нет — причина: …` с названным инвариантом, отпечаток `e603b8ee`),
  комплект пересобран.

## V3.1 · 2026-09-21

Правки по итогам прогона run4 — агент консолидации финансовой отчётности с
расчётным ядром (`runs/2026-09-21_sonnet_run1/run4_consolidation/`).
Сборщик на Sonnet сам пришёл к harness (правила yaml, движок, 16 тестов,
эталон до рубля после независимого пересчёта, физический стоп при
BLOCKING); приёмщик вслепую — PASS 4/5; все дефекты — фабрики, не ядра.

- `build_agent_package.py`: подтверждённые инструменты команды запуска и
  команды `N.1` порождаются из блока 11 («Коннекторы:», «Навыки
  пространства:»), а не «нет» для всех; шаги-ветви («При …», «Если …»,
  «Иначе …») помечены «(ветвь)» и берут вход у точки ветвления; строка
  проверок модулей — один раз; списки команд без «.;» и «..».
- `spec_readiness.py`: `Ф-11-КОННЕКТОР` — коннектор упомянут словами без
  строки «Коннекторы:» (оба порога).
- `templates/AGENT_SPEC.template.md`, `references/agent_spec.md`: блок 11 —
  обязательные подмаркеры «Навыки пространства:» и «Коннекторы:» («нет»,
  а не молчание).
- `factory_common.workspace_skills()`: «нет» не читается как имя навыка.
- `build_delivery.py`: `--расчётная` / фронтматтер `расчётная: да` →
  сверка с Я10; `[WARN] Д3` в `СБОРКА.md`, когда запасной результат —
  заготовка; при расчётной задаче — шаг инструкции по сборке про
  размещение ядра в песочнице коннектора; зачин блока активации при
  навыках пространства — «Активируй последовательно N навыка» (форма
  сверки Я4).
- `selftest.py`: 98 проверок (+5). `selftest_delivery.py` 403/403,
  `mutate_landed` 112/112, `mutate_build` 6/6.
- Пример `1c-dev-assistant`: спецификация S6 (блок 11 с подмаркерами,
  отпечаток `6ba73507`), комплект V5 пересобран (PASS 55/55 + 11/11).
- `docs/03`: вопрос 9 к платформе — как код ядра попадает в песочницу
  коннектора исполнения кода.
- Публикация (2026-09-22): репозиторий открыт под MIT на GitHub
  (`sergiopoddubniy/agent-factory-gigacowork`). Из репозитория убран план
  пилота у клиента, имена клиентов в уроках справочников, скриптов и
  прогонов заменены на условные (Клиент-А, Клиент-П, Клиент-М,
  ДатаРеестр); README переписан для внешнего читателя, добавлены
  `LICENSE`, `CONTRIBUTING.md`, CI (`.github/workflows/ci.yml`: selftest,
  selftest_delivery, бюджет оркестратора, стенды мутаций на Python 3.10 и
  3.12). `mutate_budget.py`: мутация «совещательный язык» смягчает
  оркестратор целиком (SKILL.md + pipelines) — 4/4 вместо 3/4.

## V3 · 2026-09-21

Широкая постановка: фабрика — общее решение для разных клиентов и агентов.
Перенесён исходный скилл `gigacowork-agent-scenario` v9.10 целиком; комплект
поставки порождается из спецификации по целевому составу.

- Справочники: 30 файлов исходного скилла дословно (+ `agent_templates/`,
  `knowledge/`); `output_stability.md` — полный текст с приложением фабрики;
  `references/pipelines/` — стадии трёх режимов текстом исходного
  оркестратора со вставками про спецификацию.
- Скрипты: 20 исходных дословно (`check_delivery.py`, `check_run.py`,
  `agent_registry.py`, `templates.py`, `context_quality.py`, `findability.py`,
  `freeze_release.py`, `migrate_layout.py`, `init_client_project.py`,
  `build_agent_card.py`, `token_budget.py`, `audit_findings.py`,
  `promote_finding.py`, `mutate_*.py`, `textnorm.py`, `docx_helpers.js`;
  исходный `build_agent_package.py` → `pack_agent_package.py`; исходный
  `selftest.py` → `selftest_delivery.py`, 403/403). Фабричный аудит пакета
  переименован в `check_package.py`.
- Новые: `build_delivery.py` — комплект поставки из спецификации (Я1–Я21,
  надстройки Д/В/П, архив вне поставки, три строки проверок в `СБОРКА.md`,
  пакет доказательств); `docx_writer.py`, `xlsx_writer.py` — документы на
  stdlib по `design_system.md`.
- Спецификация: подмаркеры блока 6 «База знаний:», «Команды:»; блока 11
  «Навыки пространства:», «Коннекторы:»; в блоке 12 «· Признаки: …» на
  языке `check_run.py`. Сборщик порождает раздел 0 активации навыков
  пространства, фиксированный ответ Режима 0 из пяти блоков, команды
  `N.0`/`N.1`/… с телами, карточку с `**id:**` и блоком активации.
- Оркестратор `SKILL.md` пересобран: правила, режимы, задачи, маршрут; в
  норме бюджета контекста (4,9 тыс. токенов). Команды: + `/комплект`,
  `/портфель`, `/пилот`.
- Пример S5 (отпечаток `4202743f`): комплект `examples/1c-dev-assistant/комплект/`
  проходит сверку исходного `check_delivery.py` — демонстрация 55/55 + 11/11,
  воркшоп 55/56 + 10/10, пилот 55/56 + 21/21; `check_run.py` на ответах
  прогона — блокирующих провалов 0; база шаблонов начата этим примером.
- Реестр реализованных агентов в репозитории пуст: клиентские данные в Git
  не несём, наполняется сканированием на машине консультанта.
- `selftest.py`: 93 проверки (+37): комплект поставки, три задачи, мутации,
  порт каждого скрипта, связанность оркестратора.
- Документы: `01_Архитектура`, `05_Сверка_с_исходным_скиллом`, README.

## V2 · 2026-09-21

Воспроизводимость результата как порождаемая структура; сверка с исходным
скиллом. Основание — прогоны 1–3б на Sonnet (`runs/2026-09-21_sonnet_run1/`).

- Блок 8 спецификации: подмаркер `Чек-лист полноты:`; каждый пункт с
  критерием закрытия `— закрыто, если: …` (`factory_common.checklist`,
  `checklist_items`). Ворота: `Ф-08-ЧЕКЛИСТ` (≥5 пунктов) и `Ф-08-КРИТЕРИЙ`
  (критерий у каждого пункта) на пороге pilot для классов ии-нативный и
  агентный.
- Сборщик порождает из чек-листа: таблицу критериев и форму таблицы
  результата в §8 (три статуса `закрыто` / `вопрос` / `конфликт`, якорь,
  номер вопроса, строка сверки K+Q+C=N), модуль 0 «опись» в §6, первый и
  второй проход в §7, одностороннее правило о расхождениях в §10, гейт
  `completeness_check` в §14, пункт 0 формата ответа в §17. Пункт без
  критерия получает критерий по умолчанию.
- Шлюз на источник входных данных в §7 и `run-agent.md` переформулирован
  по исходному скиллу: спрашивать только если источник не очевиден;
  приложенные файлы — работать без переспроса (дефект нашёлся прогоном 3).
- Новый справочник `output_stability.md` (сжатие одноимённого справочника
  исходного скилла + таблица «что показали прогоны S1→S4»); обновлены
  `agent_spec.md`, `evals_and_acceptance.md` (класс `same_input_twice`),
  шаблон спецификации, `SKILL.md` (интервью: чек-лист и критерии),
  команда `/приёмка`.
- `selftest.py`: 56 проверок (+12): мутации чек-листа и критериев, форма
  порождённого навыка, шлюз на источник, критерий по умолчанию.
- Пример `1c-dev-assistant`: спецификация S4 (отпечаток `456170b9`),
  комплект V4, прогон из 10 случаев (все PASS, `same_input_twice` —
  10/10 статусов в трёх сессиях), пакет доказательств READY по demo.
- Документы: `05_Сверка_с_исходным_скиллом.md` (20 скриптов, 30
  справочников: перенесено / осознанно нет / стоит перенести), обновлены
  01, 04, README.
- Прогоны: `runs/2026-09-21_sonnet_run1/run3/` — S3/V3 (8 PASS, 2 FAIL) и
  `run3/S4/` — S4/V4 (10 PASS), отчёт `ОТЧЁТ_ПРОГОНА_run3.md`.


## V1 · 2026-09-21

Первая версия. Режим одиночного агента.

- Спецификация агента `AGENT_SPEC.md`: фронтматтер + 15 блоков, статусы
  заполнено / допущение / отсутствует, строки `Допущение:` и `Не знаем:`.
- Скрипты (stdlib, Python 3.9+): `factory_common.py`, `spec_readiness.py`
  (пороги demo/pilot), `consulting_plan.py`, `build_agent_package.py`
  (порождение из спецификации + аудит + детерминированный архив),
  `check_delivery.py` (аудит + сверка отпечатка), `evidence_bundle.py`,
  `selftest.py` (44 проверки, мутации).
- Оркестратор `SKILL.md` (7 шагов), 6 команд, 6 справочников, 2 шаблона.
- Пример `1c-dev-assistant`: спецификация, прогон, план добора, комплект.
- Документы: концепция, архитектура, пилот у клиента, валидация для
  платформы, руководство по внедрению Agent Spec в исходный скилл.

Основание: скилл `gigacowork-agent-scenario` v9.10 (9b8dd938), white paper
AI-DISRUPT PDLC v3.7, практика Spec-Driven Development.
