# Переиспользование — след

Спецификация demo-1c-dev-assistant · S8 · e603b8ee

Что рассмотрено (реестр реализованных агентов `agent_registry.py --похожие`, база шаблонов `templates.py --похожие`, `examples/`), что взято, что не взято и почему (`delivery_composition.md`, Я21):

Рассмотрены: шаблон «подготовка проекта документа» (приём источников → извлечение фактов → draft → контроль placeholders → выдача) — взято как основа модулей; шаблон «поиск по базе знаний» — взято для ответов о конфигурации. Не взято: «сравнение документов» — причина: задача не про версии одного документа, а про запрос против конфигурации. Реестр реализованных агентов (`agent_registry.py --похожие`): близкого агента 1С не найдено, кандидатов нет — сказано явно.
