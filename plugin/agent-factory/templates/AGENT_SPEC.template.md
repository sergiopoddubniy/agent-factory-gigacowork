---
id: <клиент>-<джоба>
имя агента: <русское имя, как его увидит пользователь в каталоге навыков>
версия спецификации: S1
обновлено: ГГГГ-ММ-ДД
класс применимости: <детерминированный | ии-помощник | ии-нативный | агентный>
контур: demo
уровень доступа: L0
категория: <категория в каталоге или «Без категории»>
---
# <то же имя, что в поле «имя агента»>

## 1. Возможность

<Зачем нужен агент: что меняется, для кого, почему сейчас. Одна фраза о результате, а не о функции. Кто заинтересован.>

## 2. PR/FAQ

**Пресс-релиз из будущего.** «<Полстраницы: что изменилось для пользователя после успешного запуска, глазами первого пользователя. Сначала результат, потом технология.>»

**Вопросы первых пользователей.**
- «<вопрос>» — <ответ>
- «<вопрос>» — <ответ>
- «<вопрос>» — <ответ>

## 3. Гипотеза результата

- Метрика: <что измеряется и в каких единицах; не «улучшить»>
- Бейзлайн: <текущее значение и как измерено: хронометраж, журнал, за какой период>
- Цель: <целевое значение и защитная метрика, ниже которой результат не засчитывается>
- Опережающий индикатор: <что сдвинется раньше основной метрики>
- Период подтверждения: <7 / 14 / 28 / 90 дней и на скольких задачах>
- Условие пересмотра: <при каком значении к какому дню гипотеза считается не подтверждённой и что делаем>

## 4. Адаптация или перепроектирование

- Решение: <адаптация | перепроектирование>
- Обоснование: <какие шаги процесса остаются, какие берёт агент, какие исчезают; почему не другой вариант>

## 5. Пользователь и триггер

- Роль: <должность, отдел>
- Триггер: <что запускает работу агента>

## 6. Входы и выходы

Входы:
- <документ или данные, формат>
- <…>

Выходы:
- <результат агента>
- <…>

## 7. Периметр

In-scope:
- <что агент делает — одной фразой на пункт>

Out-of-scope:
- <соседний запрос, который легко перепутать, и который агент НЕ берёт>
- <…>

## 8. Правила и критерии решений

- <признак → порог → что делать при неоднозначности>
- <…>
- Любое расхождение между запросом и данными — всегда `конфликт` и `conflict_requires_review`, даже при правдоподобном объяснении; объяснение — рядом как гипотеза.

Чек-лист полноты:
1. <что агент обязан закрыть по каждому запросу: объект и реквизит> — закрыто, если: <источник называет объект и он найден в данных>
2. <кто выполняет каждое действие> — закрыто, если: <для каждого действия из запроса названа роль исполнителя; «очевидный» исполнитель критерий не выполняет>
3. <ожидаемый результат для пользователя> — закрыто, если: <в запросе есть формулировка результата, а не только функции>
4. <тип и длина поля / параметры> — закрыто, если: <названы явно; для запросов без поля — «не применяется: причина»>
5. <обязательность и момент проверки> — закрыто, если: <сказано, обязательно ли и на каком событии>
6. <права доступа к новому> — закрыто, если: <названы роли или сказано «права не меняются»; «кто видит поле» критерий не выполняет>
7. <существующие данные: заполнять или нет (миграция)> — закрыто, если: <сказано явно; молчание — `вопрос` всегда>
8. <расхождения запроса с данными> — закрыто, если: <всё названное сверено и расхождений нет; любое расхождение — `конфликт`>

Шаги:
1. <действие> → <что получилось на выходе шага>
2. <…>

## 9. Неопределённость и эскалация

- Когда <условие>, агент должен <ответ>.
- Когда <условие>, агент должен <ответ>.
- Когда <условие>, агент должен <ответ>.

## 10. HITL-карта

| Точка | Кто | Критерий | Что блокирует | Эскалация |
|---|---|---|---|---|
| <на каком шаге> | <роль> | <по какому критерию> | <какое действие невозможно без подтверждения> | <кому, если разногласие> |

## 11. Доступ и guardrails

- Уровень <L0–L3>: <что доступно>
- <запреты: запись наружу, персональные данные, что не покидает пространство>

Навыки пространства: <навык — зачем> | нет
Коннекторы: <имя — что даёт — версия x.y> | нет

## 12. Приёмочные случаи

- `happy_path` — Вход: <…> → Ожидание: <…>
- `missing_required_input` — Вход: <…> → Ожидание: <…>
- `conflicting_sources` — Вход: <…> → Ожидание: <…>
- `prompt_injection_in_document` — Вход: <…> → Ожидание: <…>
- `external_action_without_confirmation` — Вход: <…> → Ожидание: <…>
- `<предметный_случай_1>` — Вход: <…> → Ожидание: <…>
- `<предметный_случай_2>` — Вход: <…> → Ожидание: <…>
- `same_input_twice` — Вход: тот же вход в двух сессиях с нуля → Ожидание: число строк чек-листа совпадает точно; статус — не менее чем у 90 % строк; расхождения локализованы по якорям; ни одно не разрешено агентом самостоятельно

## 13. Эталонные разборы

- <файл или описание: документ + правильный результат, заполненный экспертом>
- <…>

## 14. Переиспользование

<Что рассмотрено (реестр агентов, база шаблонов), что взято, что отвергнуто и почему. Пустой список — тоже строка, а не молчание.>

## 15. Владелец и приёмка

- Владелец процесса: <роль>
- Принимает: <кто и по каким случаям>
- Дата приёмки: <…>
