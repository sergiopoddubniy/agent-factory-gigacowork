# Реестр реализованных агентов. Единственный источник.
# Генерируется `scripts/agent_registry.py --сканировать "<папка клиента>" …`.
# Поле «эффект» правится ТОЛЬКО человеком и только со ссылкой
# на замер: на него ссылаются в продаже.
#
# В репозитории фабрики реестр пуст: он наполняется на машине консультанта
# сканированием папок клиентов и клиентских данных в Git не несёт.
[]
