---
name: build
summary: Сборка пакета агента из спецификации — SKILL.md, run-agent.md, архив, аудит.
version: 1.0.0
---

# /собрать

**Версия:** V1 · 2026-09-21

Активируй навык «Фабрика агентов GigaCowork» и выполни шаг 4 — сборку пакета.

## Параметры запуска
Название задачи: сборка пакета агента
Контур: demo (из фронтматтера спецификации)
Спецификация: AGENT_SPEC.md в рабочей папке
Папка комплекта: ./комплект
Версия агента: V1 (при пересборке после правок — следующая: V2, V3 …)

## Правила выполнения
- запустить `python3 scripts/build_agent_package.py --spec AGENT_SPEC.md --out ./комплект --version V<N>`;
- показать вывод аудита дословно; при FAIL — архив не создаётся, и это не обходится: правится спецификация;
- показать пользователю открытые файлы пакета (`SKILL.md`, `run-agent.md`) и объяснить, что править их руками нельзя — следующая сборка перепишет, а аудит увидит несовпадение отпечатка;
- объяснить загрузку по `references/package_format.md`: архив в конструктор (Поколение 2) или поля формы «Создать навык» (Поколение 1.5), один в один, без перевода;
- закончить `/приёмка`.
